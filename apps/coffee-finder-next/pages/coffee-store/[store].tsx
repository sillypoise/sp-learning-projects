import { NextPage } from "next";

const Store: NextPage = ({}) => {
    return (
        <article>
            <h1>Dyanamic coffee store page!</h1>
        </article>
    );
};

export default Store;
